name: "Orchestrator: Watchdog"

on:
  schedule:
    - cron: "17 */6 * * *"

# One repo-wide non-canceling authority lock shared by evaluator and orchestrator; GitHub keeps only the newest pending run per group and the six-hourly watchdog is the convergence net.
concurrency:
  group: automation-authority-${{ github.repository }}
  cancel-in-progress: false

jobs:
  watchdog:
    permissions:
      checks: write
      contents: read
      issues: write
      pull-requests: write
    uses: kgsmith19/agent-engineering-standard/.github/workflows/pr-automation-reusable.yml@__STANDARD_SHA__
    with:
      mode: watchdog
      standard_sha: __STANDARD_SHA__
    secrets:
      automation_token: ${{ secrets.AUTOMATION_TOKEN }}
